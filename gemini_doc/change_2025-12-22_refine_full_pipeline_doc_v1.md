# 变更记录：优化 boundflow_full_pipeline_from_claims_to_ae.md 文档质量

## 动机

经过全面审查，`gemini_doc/boundflow_full_pipeline_from_claims_to_ae.md` 文档整体质量很高，引用准确，结构清晰。但发现以下可改进点：

1. **Mermaid 图表语法问题**：部分节点使用双引号包裹可能导致解析器兼容性问题
2. **文档元信息缺失**：缺少版本号、更新时间、对应状态等追溯信息
3. **行号引用维护风险**：大量行号引用在代码重构后可能失效
4. **部分引用表述可优化**：6.2 节对 phase5E.md 的引用稍显隐晦
5. **TODO 部分结构性不足**：缺少优先级分类与具体指导
6. **3.1 节编号说明不清**：读者可能误以为有 3.2、3.3 等子节

## 本次改动

### 1) 增加文档元信息（高优先级）

在文档开头增加：
- 文档版本：v1.0（对应 schema_version=1.0）
- 最后更新时间：2025-12-22
- 对应状态：Phase 5 完成声明链接
- 行号引用风险提示：说明行号基于当前代码状态，建议以函数名为准

**修改位置**：第 1-9 行

### 2) 修复 Mermaid 图表语法（高优先级）

将节点 D 和 H 的双引号语法改为方括号：
- `D["Planner: plan()"]` → `D[Planner: plan]`
- `H["TVM Backend Lowering/Compile (internal)"]` → `H[TVM Backend Lowering/Compile - internal]`

**修改位置**：第 32-47 行（系统架构图）

**理由**：提升 Mermaid 解析器兼容性，避免部分渲染环境（如某些 Markdown 预览器）解析失败。

### 3) 增加 3.1 节说明（中优先级）

在 3.1 节开头增加说明块：
> **说明**：本节（3.1）是对上述架构图与时序图的学术视角解读，说明系统设计如何支撑论文级的可验证性、可消融性与可复现性。

**修改位置**：第 90-92 行

**理由**：明确本节定位，避免读者误以为有 3.2、3.3 等并列小节。

### 4) 优化 6.2 节文件引用（低优先级）

将原描述：
```markdown
4. 更新 `docs/bench_jsonl_schema.md`，并按 `docs/phase5E.md:31` 提示 bump `schema_version`。
```

改为：
```markdown
4. 更新 `docs/bench_jsonl_schema.md` 中的字段说明。如需 bump `schema_version`，参考 `docs/phase5E.md` 的 schema 演进指南。
```

**修改位置**：第 303 行

**理由**：去掉具体行号引用，改为更明确的描述性引用。

### 5) 结构化 TODO 部分（高优先级）

将原有扁平列表改为：
- **高优先级（影响论文/AE 交付）**
  - Phase 0 claims 文档（含详细建议内容：5 条具体要求）
- **中优先级（Phase 6 扩展）**
  - 论文图表口径定稿
  - 大规模 workload
  - 更强 domain
  - 自动化 CI/CD

**修改位置**：第 357-377 行

**理由**：
- 分类明确优先级，便于后续工作规划
- 为 Phase 0 claims 提供具体建议内容（5 条可操作指南）
- 新增"自动化 CI/CD"作为中优先级待补齐项

## 验证

文档修改为纯文本变更，无需运行时验证。可通过以下方式确认：
1. Markdown 预览器渲染 Mermaid 图表正常
2. 文档元信息清晰可见
3. TODO 部分结构清晰、优先级明确

## 备注

- 本次修改不涉及代码逻辑，仅优化文档可读性与可维护性
- 所有行号引用保持不变（但增加了风险提示）
- 文档版本号与 schema_version 保持一致（v1.0）

