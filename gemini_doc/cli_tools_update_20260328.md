# Codex与Claude Code更新记录

## 更新时间
2026年03月28日

## 更新内容
1. **@openai/codex**:
   - 使用 `npm update -g @openai/codex` 将本地全局 npm 上的 Codex 更新到最新版本。
2. **Claude Code (claude)**:
   - 识别出 `claude` 位于 `~/.local/bin/claude`。
   - 运行了内置自带更新命令 `claude update`。
   - 成功将版本从 2.1.83 升级到 2.1.86。

## 备注
所有更新均在宿主环境顺利完成，工具状态正常。
