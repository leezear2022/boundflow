# R3-1b2 compiled P-alpha VJP

One fresh subprocess compares independent native autograd with the compiled full-lower custom Function and compressed P-alpha VJP. Replay hash-checks code and every signed file, then independently recomputes tensor hashes, numerical parity and ownership receipts. This closes only b2; b3 five-fresh memory gating, optimizer integration and timing remain separate.
