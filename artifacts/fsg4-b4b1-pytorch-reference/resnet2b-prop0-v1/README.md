# FSG4/B4-B1 Typed Pure-PyTorch Reference

Root replay reads the hash-bound B4-B1a five-fresh raw, recompiles static typed IR and per-capture instances, executes only public PyTorch operators, and compares sparse reconstruction, forward outputs, and eligible local VJP gradients. This is correctness evidence pending external audit; no TIR, performance, memory, or ASPLOS-ready claim.
