# RVIR-v4 V4-2D Native Optimizer Step Parity

This artifact executes ten native lower evaluations and nine Adam updates from the V4-2C state with zero provider callbacks, then compares every lower, alpha, and beta step to the frozen production trace. It does not perform atomic copy-out or claim performance.
