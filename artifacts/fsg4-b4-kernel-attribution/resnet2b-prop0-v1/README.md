# FSG4/B4-0 kernel attribution artifact

This artifact contains one fresh unprofiled B3 control and one fresh B3 worker observed with PyTorch CPU/CUDA profiling. Profile time is attribution-only and never a performance claim. Replay rebuilds the 14-call marker coverage, raw kernel aggregation, and Amdahl gates. Tamper probes rewrite and re-sign outer artifact digests.
