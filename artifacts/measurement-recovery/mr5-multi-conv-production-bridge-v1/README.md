# MR5 Multi-Conv Production Bridge Formal Artifact

Five fresh provider/bridge pairs plus one C1 mutation-after-failure rollback probe.
No timing is recorded and no performance claim is made.

The manifest also binds the losslessly compressed `raw.json.xz` and a 21-case fully
re-signed tamper report. Replay decompresses raw worker payloads and recomputes all
pair metrics; it does not trust the frozen summary.
